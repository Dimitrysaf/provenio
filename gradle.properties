org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

kotlin.code.style=official

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.mpp.androidSourceSetLayoutVersion=2

# Single source of truth for the app's version. Bump this by hand for a
# release/feature/fix. CI (.github/workflows/prerelease.yml) reads it and
# appends "-<commit count>" for prerelease/test builds instead of hardcoding
# its own version.
provenioVersion=0.43.0
